<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- <typeAliases> -->
	<!-- # 定义单个别名 -->
	<!-- <typeAlias type="com.domain.User" alias="user"/> -->
	<!-- # 批量的别名定义，扫描整个包下面的所有类，别名为类名（首字母大小写都可以） -->
	<!-- <package name="com.po"/> -->
	<!-- <package name="其他包"/> -->
	<!-- </typeAliases> -->
	<settings>
		<!-- 查询时，关闭关联对象即时加载以提高性能 是否启用延迟加载机制，默认值true -->
		<setting name="lazyLoadingEnabled" value="true" />
		<!-- true启用时，当延迟加载开启时访问对象中一个懒对象属性时，将完全加载这个对象的所有懒对象属性。false，当延迟加载时，按需加载对象属性（即访问对象中一个懒对象属性，不会加载对象中其他的懒对象属性）。默认为true -->
		<setting name="aggressiveLazyLoading" value="true" />
		<setting name="cacheEnabled" value="false" />
		<!-- 打印sql语句 -->
		<!-- -->
		<setting name="logImpl" value="STDOUT_LOGGING" />
	</settings>
	<typeAliases>
		<package name="com.seax" />
	</typeAliases>
</configuration>